<%= form_for(@forecast_type) do |f| %>
  <% if @forecast_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forecast_type.errors.count, "error") %> prohibited this forecast_type from being saved:</h2>

      <ul>
      <% @forecast_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
	
	<table>
	  <tr>
	    <th>Name</th>
	    <th>Abbr</th>
	  </tr>
		<tr>
			<td>
			<%= f.text_field :name %>
			</td>
			<td>
				<%= f.text_field :abbreviation %>
			</td>
		</tr>
	</table>	

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
